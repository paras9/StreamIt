"use client"

interface ChatCommunityProps {
    hostName: string;
    viewerName: string;
    isHidden: boolean;
};

export const ChatCommunity =({}: ChatCommunityProps) =>{
    return(
        <div className=""></div>
    )
}